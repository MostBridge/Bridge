name: Run container

on: 
  workflow_dispatch:

jobs: 
  Run:
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands to deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            sudo docker image ls
            sudo docker container ls --all
            sudo docker rmi 73a99bed4243
            sudo docker rmi 14ac9a8d7fff
            sudo docker rmi 9ade97530207
            sudo docker image ls 
